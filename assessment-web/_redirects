# Learner links are /assess/:token, /satisfaction/:token and /tracer/:token —
# the token isn't a file, so serve the app shell and let app.js read it.
/assess/*        /index.html  200
/satisfaction/*  /index.html  200
/tracer/*        /index.html  200
